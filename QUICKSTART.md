# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Запуск веб-сервера

```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

## Шаг 3: Откройте браузер

Перейдите по адресу: **http://localhost:5000**

Нажмите кнопку **"🎲 Провести розыгрыш"** и увидите результаты!

---

## Альтернатива: Демонстрация через командную строку

Если хотите протестировать без веб-сервера:

```bash
python demo.py
```

Этот скрипт покажет полный процесс розыгрыша в консоли.

---

## Настройка билетов

Отредактируйте файл `tickets.json`:

```json
{
    "tickets": [666, 77, 123, 1, 6, 1234]
}
```

---

## Проверка работы

После запуска сервера вы можете:

1. **Веб-интерфейс**: Откройте http://localhost:5000
2. **API**: Используйте `/api/lottery/draw` для программного доступа
3. **Демо**: Запустите `python demo.py` для консольной демонстрации

---

## Возможные проблемы

### Ошибка подключения к Bitcoin API

Если нет интернета или API недоступен, система использует тестовые хеши блоков.

### Порт 5000 занят

Измените порт в файле `app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=8080)  # Используйте другой порт
```

---

Готово! 🎉

