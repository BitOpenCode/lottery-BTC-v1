# 🚀 Настройка GitHub Pages

Этот документ описывает процесс развертывания проекта BTC Lottery через GitHub Pages.

## ⚠️ Важное примечание

GitHub Pages поддерживает только **статические сайты** (HTML, CSS, JavaScript). Flask приложение (бэкенд) **не может** быть развернуто на GitHub Pages.

## 📋 Решение: Раздельное развертывание

Проект разделен на две части:
1. **Фронтенд** (статический) → GitHub Pages
2. **Бэкенд** (Flask API) → Отдельный хостинг (Render, Heroku, Vercel и т.д.)

## 🔧 Шаг 1: Настройка GitHub Pages

### 1.1. Включение GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе **Source** выберите:
   - **Branch**: `main`
   - **Folder**: `/docs`
3. Нажмите **Save**

### 1.2. Проверка

После сохранения GitHub автоматически опубликует сайт. URL будет:
```
https://bitopencode.github.io/lottery-BTC-v1/
```

## 🔧 Шаг 2: Развертывание бэкенда

Бэкенд нужно развернуть на платформе, поддерживающей Python/Flask.

### Вариант A: Render (Рекомендуется)

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите репозиторий GitHub
4. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`
   - **Environment**: `Python 3`
5. Добавьте переменные окружения (если нужно)
6. Нажмите **Create Web Service**

После развертывания вы получите URL вида:
```
https://lottery-btc-backend.onrender.com
```

### Вариант B: Heroku

1. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
2. Создайте файл `Procfile`:
   ```
   web: python app.py
   ```
3. Войдите в Heroku:
   ```bash
   heroku login
   ```
4. Создайте приложение:
   ```bash
   heroku create lottery-btc-backend
   ```
5. Разверните:
   ```bash
   git push heroku main
   ```

### Вариант C: Vercel (с Serverless Functions)

Vercel поддерживает Python через Serverless Functions. См. документацию Vercel.

## 🔧 Шаг 3: Настройка CORS

Убедитесь, что бэкенд настроен для работы с CORS. В файле `app.py` уже есть:

```python
from flask_cors import CORS
CORS(app)
```

Это позволяет фронтенду на GitHub Pages делать запросы к бэкенду.

## 🔧 Шаг 4: Обновление API URL во фронтенде

После развертывания бэкенда обновите `API_BASE` в файле `docs/static/js/app.js`:

```javascript
const API_BASE = 'https://your-backend-url.com';
```

Или используйте переменную окружения для динамической настройки.

## 📝 Структура для GitHub Pages

```
docs/
├── index.html          # Главная страница
├── .nojekyll          # Отключает Jekyll обработку
└── static/
    ├── css/
    │   └── style.css
    └── js/
        └── app.js
```

## ✅ Проверка развертывания

1. **Фронтенд**: Откройте `https://bitopencode.github.io/lottery-BTC-v1/`
2. **Бэкенд**: Проверьте API: `https://your-backend-url.com/api/lottery/tickets`

## 🔄 Автоматическое обновление

При каждом push в ветку `main`:
- GitHub Pages автоматически обновит фронтенд
- Render/Heroku автоматически обновит бэкенд (если настроен автодеплой)

## 🐛 Решение проблем

### Проблема: CORS ошибки

**Решение**: Убедитесь, что в `app.py` включен CORS:
```python
from flask_cors import CORS
CORS(app)
```

### Проблема: API не отвечает

**Решение**: 
1. Проверьте, что бэкенд запущен
2. Проверьте URL в `API_BASE`
3. Проверьте логи бэкенда

### Проблема: Статические файлы не загружаются

**Решение**: Убедитесь, что пути в `index.html` правильные:
```html
<link rel="stylesheet" href="static/css/style.css">
<script src="static/js/app.js"></script>
```

## 📚 Дополнительные ресурсы

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Render Documentation](https://render.com/docs)
- [Heroku Python Guide](https://devcenter.heroku.com/articles/getting-started-with-python)

---

**Примечание**: Для продакшн использования рекомендуется:
- Использовать HTTPS для обоих фронтенда и бэкенда
- Настроить мониторинг и логирование
- Добавить rate limiting для API
- Настроить резервное копирование данных

