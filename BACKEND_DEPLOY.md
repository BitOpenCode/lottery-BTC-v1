# 🚀 Быстрое развертывание бэкенда для решения CORS

## ⚠️ Проблема

При открытии приложения в Telegram Mini App возникает ошибка CORS:
```
Access to fetch at 'https://...' from origin 'https://bitopencode.github.io' has been blocked by CORS policy
```

Это происходит потому, что бэкенд не развернут или не настроен правильно.

## ✅ Решение: Развертывание на Render.com (5 минут)

### Шаг 1: Регистрация
1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создание Web Service
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий `lottery-BTC-v1`
3. Настройки:
   - **Name**: `lottery-btc-backend` (или любое другое)
   - **Region**: Выберите ближайший
   - **Branch**: `main`
   - **Root Directory**: `.` (корень)
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`

### Шаг 3: Переменные окружения (опционально)
Не требуются для базовой работы.

### Шаг 4: Создание
1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (2-3 минуты)

### Шаг 5: Обновление фронтенда
После развертывания вы получите URL вида:
```
https://lottery-btc-backend.onrender.com
```

Обновите файл `docs/static/js/app.js`:

```javascript
const API_BASE = window.location.hostname === 'bitopencode.github.io' 
    ? 'https://lottery-btc-backend.onrender.com'  // Ваш URL
    : '';
```

И закоммитьте изменения:
```bash
git add docs/static/js/app.js
git commit -m "Обновлен API_BASE для работы с развернутым бэкендом"
git push
```

## 🔄 Альтернатива: Локальный бэкенд для тестирования

Если нужно протестировать локально:

1. Запустите бэкенд локально:
   ```bash
   python app.py
   ```

2. Используйте ngrok для туннеля:
   ```bash
   ngrok http 8080
   ```

3. Обновите `API_BASE` на URL от ngrok

## ✅ Проверка

После развертывания проверьте:
1. Бэкенд доступен: `https://your-backend.onrender.com/api/lottery/tickets`
2. CORS работает: Откройте приложение в Telegram Mini App
3. Розыгрыш работает: Добавьте билеты и проведите розыгрыш

## 🐛 Решение проблем

### Бэкенд не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что порт в `app.py` правильный (Render использует переменную `PORT`)

### CORS все еще не работает
- Убедитесь, что в `app.py` настроен CORS правильно
- Проверьте, что бэкенд действительно запущен и доступен
- Проверьте логи бэкенда на наличие ошибок

### Telegram Mini App не подключается
- Убедитесь, что URL бэкенда правильный в `API_BASE`
- Проверьте, что бэкенд доступен по HTTPS
- Убедитесь, что бэкенд отвечает на запросы (проверьте через браузер)

