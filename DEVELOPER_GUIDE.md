# 📘 Руководство разработчика: BTC Lottery

## 🎯 Смысл и концепция лотереи

### Что это такое?

BTC Lottery — это честная, прозрачная и полностью проверяемая система лотереи, использующая блокчейн Bitcoin как источник непредсказуемой случайности. Система гарантирует честность без необходимости доверия к организатору.

### Зачем это нужно?

1. **Честность без доверия**: Результат определяется публичными данными из блокчейна Bitcoin, которые невозможно подделать или предсказать заранее
2. **Прозрачность**: Любой участник может самостоятельно проверить правильность выбора победителя
3. **Детерминированность**: Одинаковые входные данные всегда дают одинаковый результат
4. **Невозможность манипуляций**: Организатор не может влиять на результат, так как хеши блоков генерируются майнерами Bitcoin

---

## 📋 Правила лотереи

### Основные принципы

1. **Список билетов фиксируется заранее**
   - Номера билетов могут быть произвольными (не обязательно последовательными)
   - Список публикуется до начала розыгрыша
   - Каждый билет имеет уникальный номер

2. **Источник случайности — блокчейн Bitcoin**
   - Используются хеши реальных блоков Bitcoin
   - Блоки выбираются в момент розыгрыша (обычно последние 3 блока)
   - Хеши блоков невозможно предсказать или изменить

3. **Выбор победителя**
   - Для каждого билета вычисляется уникальный score на основе seed и номера билета
   - Победитель — билет с минимальным score
   - При коллизии используется tie-breaker

4. **Проверяемость**
   - Все данные публикуются: seed, список билетов, score каждого билета
   - Любой может повторить расчёт и проверить результат

---

## 🔢 Алгоритм расчета

### Шаг 1: Получение блоков Bitcoin

```python
# Получаем последние 3 блока из блокчейна Bitcoin
block_hashes = [
    "00000000000000000000f2c6261af99ee5eb24165b523a3297fb2891b644955a",  # Блок N
    "00000000000000000000f2b336400e807c58420b7eebb1fb5f5d039c58fece91",  # Блок N-1
    "00000000000000000000555d5d7b9843badbe3a80877b0f543e3d8acd8a86de5"   # Блок N-2
]
```

**Источник**: Публичный API Blockstream (`https://blockstream.info/api/`)

### Шаг 2: Генерация seed

```python
# Конкатенируем хеши блоков
concatenated = block_hashes[0] + block_hashes[1] + block_hashes[2]

# Вычисляем SHA256
seed = SHA256(concatenated)
```

**Результат**: 32-байтовый seed (64 символа в hex формате)

### Шаг 3: Вычисление score для каждого билета

#### Что такое Score?

**Score** — это большое целое число, уникальное для каждого билета, вычисленное детерминированным способом на основе seed и номера билета. Score определяет шансы билета на победу: чем меньше score, тем выше шанс выиграть.

#### Пошаговый процесс расчета Score

**Шаг 3.1: Подготовка данных**

```python
# У нас есть:
seed_bytes = bytes.fromhex(seed_hex)  # Например: b'\xa1\xb2\xc3...'
ticket_number = "666"  # Номер билета (строка)
```

**Шаг 3.2: Формирование payload**

```python
# Объединяем seed и номер билета через двоеточие
payload = seed_bytes + b":" + ticket_number.encode('utf-8')

# Пример:
# seed_bytes = b'\xa1\xb2\xc3\xd4...'
# ticket_number = "666"
# payload = b'\xa1\xb2\xc3\xd4...:666'
```

**Важно**: Двоеточие (`:`) используется как разделитель, чтобы гарантировать уникальность payload для каждого билета.

**Шаг 3.3: Вычисление SHA256 хеша**

```python
import hashlib

hash_result = hashlib.sha256(payload).digest()

# Результат: 32 байта (256 бит)
# Например: b'\x12\x34\x56\x78\x9a\xbc...' (32 байта)
```

SHA256 всегда возвращает ровно 32 байта, независимо от размера входных данных.

**Шаг 3.4: Преобразование в целое число**

```python
score = int.from_bytes(hash_result, 'big')

# 'big' означает big-endian (старший байт первым)
# Результат: очень большое целое число (до 2^256 - 1)
```

**Пример преобразования**:
```
hash_result (hex): 1234567890abcdef...
score (decimal): 1311768467463790320... (очень большое число)
```

#### Полный код функции

```python
def compute_score(seed_bytes: bytes, ticket_number: str) -> int:
    """
    Вычисляет score для конкретного билета
    
    Args:
        seed_bytes: Seed в виде байтов (32 байта)
        ticket_number: Номер билета (строка)
    
    Returns:
        int: Score билета (число от 0 до 2^256 - 1)
    """
    # 1. Формируем payload: seed + ":" + номер_билета
    payload = seed_bytes + b':' + ticket_number.encode('utf-8')
    
    # 2. Вычисляем SHA256 хеш
    hash_result = hashlib.sha256(payload).digest()
    
    # 3. Преобразуем 32 байта в целое число
    score = int.from_bytes(hash_result, 'big')
    
    return score
```

#### Примеры расчета Score

**Входные данные**:
- Seed (hex): `a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2`
- Билеты: `[666, 77, 123, 1, 6, 1234]`

**Расчет для билета №666**:
```python
# 1. Payload
seed_bytes = bytes.fromhex("a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2")
payload = seed_bytes + b":666"
# payload = b'\xa1\xb2\xc3...:666'

# 2. SHA256
hash_result = SHA256(payload)
# hash_result = b'\x12\x34\x56\x78\x9a\xbc...' (32 байта)

# 3. Score
score_666 = int.from_bytes(hash_result, 'big')
# score_666 = 1311768467463790320... (очень большое число)
```

**Расчет для билета №123**:
```python
payload = seed_bytes + b":123"
hash_result = SHA256(payload)
score_123 = int.from_bytes(hash_result, 'big')
# score_123 = 45678901234567890123... (другое большое число)
```

**Важно**: 
- Разные билеты дают разные score (из-за свойства SHA256)
- Score распределены равномерно (SHA256 обеспечивает хорошее распределение)
- Невозможно предсказать score заранее (зависит от непредсказуемого seed)

#### Почему Score работает как случайность?

1. **Равномерное распределение**: SHA256 обеспечивает равномерное распределение значений score в диапазоне [0, 2^256-1]

2. **Непредсказуемость**: Поскольку seed непредсказуем (зависит от блоков Bitcoin), score также непредсказуем

3. **Детерминированность**: Для одинаковых входных данных (seed + номер билета) всегда получается одинаковый score

4. **Уникальность**: Вероятность коллизии (два билета с одинаковым score) крайне мала: ~1/2^256

#### Визуализация процесса

```
Seed (32 байта)
    │
    ├─→ Билет №666 ──→ SHA256(seed + ":666") ──→ Score: 1311768467463790320...
    │
    ├─→ Билет №77  ──→ SHA256(seed + ":77")  ──→ Score: 23456789012345678901...
    │
    ├─→ Билет №123 ──→ SHA256(seed + ":123") ──→ Score: 45678901234567890123... ✅ MIN
    │
    └─→ Билет №1   ──→ SHA256(seed + ":1")   ──→ Score: 56789012345678901234...
```

#### Математическое объяснение

Score — это функция:
```
score(ticket) = SHA256(seed || ":" || ticket_number) mod 2^256
```

Где:
- `||` — конкатенация
- `mod 2^256` — неявно, так как SHA256 возвращает 256 бит
- Результат: целое число в диапазоне [0, 2^256 - 1]

**Свойства**:
- Инъективность: разные билеты → разные score (с очень высокой вероятностью)
- Равномерность: score распределены равномерно по всему диапазону
- Необратимость: по score невозможно восстановить номер билета или seed

### Шаг 4: Выбор победителя

```python
winner = ticket_with_minimum_score
```

**Правило**: Победитель — билет с наименьшим значением score.

### Шаг 5: Разрешение коллизий (tie-breaker)

Если два билета имеют одинаковый score (крайне редко):

```python
# Вычисляем tie-breaker для каждого билета
tie_breaker = SHA256(seed + ":" + ticket_number + ":tb1")

# Сравниваем пары (score, tie_breaker)
winner = ticket_with_minimum((score, tie_breaker))
```

---

## 📊 Пример расчета

### Входные данные

**Билеты**: `[666, 77, 123, 1, 6, 1234]`

**Блоки Bitcoin**:
- Блок #927123: `00000000000000000000f2c6261af99ee5eb24165b523a3297fb2891b644955a`
- Блок #927122: `00000000000000000000f2b336400e807c58420b7eebb1fb5f5d039c58fece91`
- Блок #927121: `00000000000000000000555d5d7b9843badbe3a80877b0f543e3d8acd8a86de5`

### Процесс

1. **Seed**:
   ```
   seed = SHA256(
       "00000000000000000000f2c6261af99ee5eb24165b523a3297fb2891b644955a" +
       "00000000000000000000f2b336400e807c58420b7eebb1fb5f5d039c58fece91" +
       "00000000000000000000555d5d7b9843badbe3a80877b0f543e3d8acd8a86de5"
   )
   ```

2. **Score для каждого билета**:
   
   Для каждого билета вычисляется score по формуле:
   ```
   score = SHA256(seed_bytes + b":" + ticket_number.encode())
   ```
   
   Затем 32 байта хеша преобразуются в целое число:
   ```
   score_int = int.from_bytes(hash_result, 'big')
   ```
   
   **Результаты** (примерные значения):
   ```
   Билет 666:  score = SHA256(seed + ":666")  → 1311768467463790320... (очень большое число)
   Билет 77:   score = SHA256(seed + ":77")   → 23456789012345678901... (другое большое число)
   Билет 123:  score = SHA256(seed + ":123")  → 45678901234567890123... ✅ (минимальный)
   Билет 1:    score = SHA256(seed + ":1")    → 56789012345678901234... (еще одно число)
   Билет 6:    score = SHA256(seed + ":6")     → 67890123456789012345... (еще одно число)
   Билет 1234: score = SHA256(seed + ":1234") → 78901234567890123456... (еще одно число)
   ```
   
   **Важно**: Все score — это очень большие числа (до 2^256 - 1). Победитель определяется простым сравнением: минимальный score выигрывает.

3. **Победитель**: Билет №123 (минимальный score)

---

## 🛠️ Что нужно для реализации

### Технические требования

#### 1. Backend (Python)

**Необходимые библиотеки**:
```python
import hashlib  # Стандартная библиотека Python
import requests  # Для HTTP запросов к Bitcoin API
```

**Основные функции**:
- Получение хешей блоков Bitcoin через API
- Генерация seed из хешей блоков
- Вычисление score для каждого билета
- Выбор победителя

**Пример структуры**:
```
lottery_core.py      # Ядро логики (SHA256, score, выбор победителя)
bitcoin_api.py       # Интеграция с Bitcoin API
app.py              # REST API сервер (Flask)
```

#### 2. Frontend (HTML/CSS/JavaScript)

**Необходимые компоненты**:
- Интерфейс для ввода билетов
- Кнопка "Провести розыгрыш"
- Отображение результатов (блоки, seed, победитель, score)
- Визуализация процесса

**API эндпоинты**:
- `POST /api/lottery/draw` - Провести розыгрыш
- `GET /api/lottery/tickets` - Получить список билетов
- `POST /api/lottery/verify` - Проверить результат

#### 3. Интеграция с Bitcoin

**Публичный API**: Blockstream (`https://blockstream.info/api/`)

**Эндпоинты**:
- `GET /api/blocks/tip/height` - Высота последнего блока
- `GET /api/block-height/{height}` - Хеш блока по высоте

**Альтернативы**:
- Blockchain.com API
- Собственный Bitcoin node (для максимальной децентрализации)

---

## 🔐 Безопасность и честность

### Гарантии честности

1. **Невозможность предсказания**
   - Хеши блоков Bitcoin генерируются майнерами случайно
   - Невозможно узнать хеш блока до его создания
   - Время создания блока непредсказуемо (в среднем 10 минут)

2. **Невозможность изменения**
   - Блоки в блокчейне Bitcoin неизменяемы
   - После создания блока его хеш фиксируется навсегда
   - Любое изменение блока изменит его хеш

3. **Прозрачность**
   - Все данные публичны: seed, билеты, score
   - Любой может проверить правильность расчёта
   - Алгоритм полностью открыт

4. **Защита от манипуляций**
   - Использование нескольких блоков (3+) усложняет манипуляции
   - Фиксация списка билетов до выбора блоков
   - Детерминированный алгоритм исключает субъективность

### Рекомендации по безопасности

1. **Фиксация списка билетов**
   - Список должен быть зафиксирован ДО получения блоков
   - Рекомендуется использовать блокчейн или публичный реестр

2. **Выбор блоков**
   - Использовать блоки, созданные ПОСЛЕ фиксации списка билетов
   - Использовать несколько блоков для дополнительной безопасности
   - Публиковать высоту блоков заранее (например, "используем блок #927123")

3. **Публикация данных**
   - Опубликовать seed, список билетов, score каждого билета
   - Предоставить инструмент для проверки результата
   - Сохранить все данные для последующей проверки

---

## 📝 Примеры использования

### Пример 1: Проведение розыгрыша

```python
from lottery_core import get_lottery_result
from bitcoin_api import get_block_hashes_for_draw

# Получаем реальные блоки Bitcoin
block_hashes = get_block_hashes_for_draw(count=3)

# Список билетов
tickets = [666, 77, 123, 1, 6, 1234]

# Проводим розыгрыш
result = get_lottery_result(block_hashes, tickets)

print(f"Победитель: билет №{result['winner']}")
print(f"Seed: {result['seed_hex']}")
print(f"Блоки: {result['block_hashes']}")
```

### Пример 2: Проверка результата

```python
from lottery_core import pick_winner

# Данные из розыгрыша
seed_hex = "a1b2c3d4e5f6..."
tickets = ["666", "77", "123", "1", "6", "1234"]
claimed_winner = "123"

# Пересчитываем
winner, scores, proof = pick_winner(seed_hex, tickets)

# Проверяем
if winner == claimed_winner:
    print("✅ Результат подтверждён!")
else:
    print(f"❌ Ошибка! Победитель должен быть {winner}, а не {claimed_winner}")
```

### Пример 3: Интеграция в игру

```python
# В вашей игровой системе
def conduct_lottery(game_tickets):
    """
    Проводит лотерею для игровых билетов
    
    Args:
        game_tickets: Список номеров билетов игроков
    
    Returns:
        dict: Результат розыгрыша
    """
    # Получаем блоки Bitcoin
    block_hashes = get_block_hashes_for_draw(count=3)
    
    # Проводим розыгрыш
    result = get_lottery_result(block_hashes, game_tickets)
    
    # Сохраняем результат в БД
    save_lottery_result(result)
    
    # Выдаём приз победителю
    winner_ticket = result['winner']
    award_prize(winner_ticket)
    
    return result
```

---

## 🎮 Интеграция в игровую систему

### Вариант 1: Простая интеграция

1. **Использовать готовый API**
   - Развернуть Flask сервер из этого проекта
   - Вызывать `/api/lottery/draw` из вашей игры
   - Получать результат и обрабатывать в игре

### Вариант 2: Встроенная интеграция

1. **Добавить модули в ваш проект**
   - Скопировать `lottery_core.py` и `bitcoin_api.py`
   - Интегрировать функции в вашу игровую логику
   - Использовать напрямую без веб-сервера

### Вариант 3: Микросервис

1. **Отдельный сервис**
   - Выделить лотерею в отдельный микросервис
   - Использовать REST API для взаимодействия
   - Масштабировать независимо от основной игры

---

## 📦 Зависимости

### Python зависимости

```txt
Flask==3.0.0          # Веб-фреймворк (опционально, если используете API)
flask-cors==4.0.0     # CORS поддержка (опционально)
requests==2.31.0       # HTTP запросы к Bitcoin API
```

### Внешние сервисы

- **Blockstream API**: `https://blockstream.info/api/`
  - Публичный, бесплатный
  - Не требует API ключа
  - Rate limit: ~1 запрос/сек

---

## 🔍 Проверка честности

### Как проверить результат

1. **Получить данные**:
   - Seed (hex строка)
   - Список билетов
   - Заявленный победитель

2. **Повторить расчёт**:
   ```python
   from lottery_core import pick_winner
   
   winner, scores, proof = pick_winner(seed_hex, tickets)
   ```

3. **Сравнить результаты**:
   - Проверить, что вычисленный победитель совпадает с заявленным
   - Проверить score каждого билета
   - Убедиться, что победитель действительно имеет минимальный score

### Онлайн проверка

Можно создать веб-инструмент для проверки:
- Ввод seed и списка билетов
- Автоматический расчёт score
- Отображение победителя и всех score

---

## 🚀 Развертывание

### Локальная разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
python app.py
```

### Продакшн

1. **Использовать production WSGI сервер**:
   ```bash
   gunicorn app:app -w 4 -b 0.0.0.0:5000
   ```

2. **Настроить HTTPS**:
   - Использовать SSL сертификаты
   - Настроить reverse proxy (nginx)

3. **Мониторинг**:
   - Логирование всех розыгрышей
   - Отслеживание ошибок API Bitcoin
   - Мониторинг производительности

---

## 📚 Дополнительные ресурсы

### Документация Bitcoin API

- Blockstream API: https://blockstream.info/api/
- Blockchain.com API: https://www.blockchain.com/api

### Блокчейн-эксплореры

- Blockstream: https://blockstream.info/
- Blockchain.com: https://www.blockchain.com/explorer
- Blockchair: https://blockchair.com/

### Криптография

- SHA256: Стандартная криптографическая хеш-функция
- Документация Python hashlib: https://docs.python.org/3/library/hashlib.html

---

## ❓ Часто задаваемые вопросы

### Q: Можно ли использовать другие блокчейны?

**A**: Да, можно использовать Ethereum, Litecoin и другие. Главное — использовать публичные данные, которые невозможно предсказать или изменить.

### Q: Что если Bitcoin API недоступен?

**A**: Рекомендуется:
1. Использовать несколько источников (backup API)
2. Кэшировать последние блоки
3. Иметь fallback механизм

### Q: Как часто можно проводить розыгрыши?

**A**: Технически — сколько угодно. Но рекомендуется:
- Использовать разные блоки для каждого розыгрыша
- Фиксировать блоки заранее
- Публиковать результаты

### Q: Можно ли использовать один блок вместо трёх?

**A**: Да, но использование нескольких блоков повышает безопасность и усложняет потенциальные манипуляции.

---

## 📞 Поддержка

Если у вас есть вопросы по реализации или нужна помощь с интеграцией, обратитесь к документации проекта или создайте issue в репозитории.

---

**Версия документа**: 1.0  
**Последнее обновление**: 2024

