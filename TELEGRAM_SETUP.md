# 📱 Настройка Telegram Mini App

## Что было сделано

Проект адаптирован под формат Telegram Mini App с современным дизайном в стиле Telegram.

## ✨ Особенности

### 1. Интеграция с Telegram Web App API
- Автоматическое определение темы (светлая/тёмная)
- Использование цветов Telegram из `themeParams`
- Поддержка главной кнопки Telegram
- Вибрация при действиях пользователя

### 2. Современный дизайн
- Стиль соответствует дизайн-системе Telegram
- Адаптивный дизайн для мобильных устройств
- Плавные анимации и переходы
- Оптимизация для touch-интерфейсов

### 3. Поддержка темной темы
- Автоматическое переключение темы
- Использование системных цветов Telegram
- Оптимизированная читаемость

## 🚀 Настройка Telegram Bot

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота

### Шаг 2: Настройка Web App

1. Отправьте команду `/newapp` боту BotFather
2. Выберите вашего бота
3. Укажите название приложения: `BTC Lottery`
4. Укажите описание: `Честная лотерея на основе Bitcoin блоков`
5. Загрузите фото (опционально)
6. Укажите URL вашего приложения: `https://your-domain.com/`
7. Сохраните полученную ссылку

### Шаг 3: Настройка кнопки меню

Отправьте BotFather команду:
```
/setmenubutton
```

Выберите бота и укажите:
- Текст кнопки: `🎰 Открыть лотерею`
- URL: `https://your-domain.com/`

## 🌐 Развертывание

### Вариант 1: Использование HTTPS сервера

1. Разверните Flask приложение на сервере с HTTPS
2. Убедитесь, что сертификат SSL валиден
3. Укажите URL в настройках бота

### Вариант 2: Использование ngrok (для разработки)

```bash
# Установите ngrok
# Запустите Flask сервер
python app.py

# В другом терминале запустите ngrok
ngrok http 8080

# Используйте полученный HTTPS URL в настройках бота
```

### Вариант 3: Использование Telegram Bot API для хостинга

Telegram предоставляет возможность хостинга статических файлов через Bot API.

## 📝 Структура URL

Приложение должно быть доступно по HTTPS URL:
```
https://your-domain.com/
```

Telegram автоматически добавит параметры:
```
https://your-domain.com/?tgWebAppStartParam=...
```

## 🔧 Настройка переменных окружения

Создайте файл `.env` (не коммитьте в git):

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBAPP_URL=https://your-domain.com/
```

## 📱 Тестирование

### Локальное тестирование

1. Запустите сервер:
```bash
python app.py
```

2. Используйте ngrok для создания HTTPS туннеля:
```bash
ngrok http 8080
```

3. Откройте ссылку в Telegram через вашего бота

### Тестирование в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите на кнопку меню или отправьте команду `/start`
3. Нажмите на кнопку "Открыть лотерею"
4. Приложение откроется внутри Telegram

## 🎨 Кастомизация цветов

Цвета автоматически берутся из настроек Telegram бота через `themeParams`. Вы можете настроить цвета в BotFather:

1. Откройте BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Appearance"
5. Настройте цвета темы

## 🔐 Безопасность

### Валидация данных от Telegram

Telegram Web App API предоставляет данные пользователя через `tg.initData`. Для продакшн использования рекомендуется валидировать эти данные на сервере.

Пример валидации (добавьте в `app.py`):

```python
import hmac
import hashlib
from urllib.parse import parse_qsl

def validate_telegram_data(init_data: str, bot_token: str) -> bool:
    """Валидация данных от Telegram"""
    try:
        parsed_data = dict(parse_qsl(init_data))
        received_hash = parsed_data.pop('hash', '')
        
        data_check_string = '\n'.join(f"{k}={v}" for k, v in sorted(parsed_data.items()))
        secret_key = hmac.new(
            key=b"WebAppData",
            msg=bot_token.encode(),
            digestmod=hashlib.sha256
        ).digest()
        
        calculated_hash = hmac.new(
            key=secret_key,
            msg=data_check_string.encode(),
            digestmod=hashlib.sha256
        ).hexdigest()
        
        return calculated_hash == received_hash
    except:
        return False
```

## 📚 Документация Telegram Web App API

- [Официальная документация](https://core.telegram.org/bots/webapps)
- [Telegram Web App API Reference](https://core.telegram.org/bots/webapps#initializing-mini-apps)

## 🐛 Отладка

### Проверка интеграции

Откройте консоль браузера в Telegram и проверьте:

```javascript
// Проверка наличия Telegram Web App API
console.log(window.Telegram?.WebApp);

// Проверка темы
console.log(window.Telegram?.WebApp?.colorScheme);

// Проверка параметров темы
console.log(window.Telegram?.WebApp?.themeParams);
```

### Логирование

В коде уже добавлено логирование для отладки. Проверьте консоль браузера для диагностики проблем.

## ✅ Чеклист запуска

- [ ] Бот создан через BotFather
- [ ] Web App настроен через BotFather
- [ ] URL приложения указан в настройках бота
- [ ] Сервер развернут с HTTPS
- [ ] Приложение доступно по указанному URL
- [ ] Тестирование в Telegram пройдено успешно
- [ ] Цвета темы настроены (опционально)

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет доступно как Telegram Mini App!

